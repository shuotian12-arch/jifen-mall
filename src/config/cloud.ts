// 云开发配置 - 替换为你的实际环境ID
export const CLOUD_CONFIG = {
  envId: 'jifen-mall-xxx', // 替换为你的云开发环境ID
  region: 'ap-shanghai'    // 可选：地域
}

// 管理员信息（临时存储，后续应使用JWT）
export const ADMIN_INFO = {
  admin_id: 'admin001',
  username: 'admin',
  token: 'temp_token'
}