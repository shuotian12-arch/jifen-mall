# 云开发配置已完成

## 已自动更新

✅ 配置文件已更新：
- 环境ID：`cloud1-d7gzjvd9od83a6a37`
- 已切换到云开发模式（`USE_MOCK_DATA = false`）
- 文件：[src/config/env.ts](src/config/env.ts)

✅ 数据库初始化脚本已生成：
- 文件：[DATABASE_INIT.json](DATABASE_INIT.json)

---

## 下一步操作

### 1. 创建数据库集合

点击左侧「**数据库**」，然后点击「+」，依次创建：

```
users
points_records
products
redemptions
admins
points_rules
operation_logs
```

### 2. 导入初始化数据

**步骤：**

1. 打开 [DATABASE_INIT.json](DATABASE_INIT.json)
2. 进入「数据库 → points_rules → 添加记录」
3. 复制粘贴第一条 JSON 数据（points_rules）
4. 点击「确定」
5. 同样方式添加 admins 集合的数据

**可选：** products 和 users 集合可以暂时留空，后面通过管理端添加。

### 3. 上传云函数

需要把云函数上传到你的微信小程序项目：

**方法一：手动复制（推荐）**

1. 将项目中的 `cloudFunctions` 文件夹复制到你的小程序项目根目录
2. 在微信开发者工具中，右键 `cloudFunctions/admin` → 上传并部署：云端安装依赖
3. 同样上传 `cloudFunctions/mp`

**方法二：通过 Git clone**

如果项目在 GitHub 上，可以直接 clone 到小程序目录。

---

完成后告诉我，我们继续测试。